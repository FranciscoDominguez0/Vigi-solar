services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      # Evitar sobreescribir los node_modules del contenedor con los del host local
      - /app/node_modules
      - /app/.next
    env_file:
      - .env
